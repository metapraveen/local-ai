use_default_settings: true

server:
  secret_key: "local-ai-searxng-secret-key-change-me"
  limiter: false

search:
  safe_search: 0
  autocomplete: "google"
  default_lang: "en"
  formats:
    - html
    - json

engines:
  - name: brave
    disabled: true
